vbox root {
  hbox services {
    node client "Client"
    node api "API Server"
  }
  hbox infra {
    node database
    node cacheServer
  }
  hbox export {
    node exportService
  }
}

edges {
  client -> api
  api -> database
  api -> exportService
  client -> cacheServer
}
